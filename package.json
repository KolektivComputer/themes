{
  "name": "kolektiv-themes",
  "version": "0.0.1-SNAPSHOT.1",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.25.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "build:packages": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "pack:packages": "pnpm -r pack",
    "publish:packages": "pnpm -r publish --access public"
  }
}
